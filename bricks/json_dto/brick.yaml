name: json_dto
description: A brick to generate a JSON Dto used with json_serializable

version: 0.1.0

environment:
  mason: ">=0.1.0-dev <0.1.0"

vars:
  name:
    type: string
    description: JSON Dto name
    prompt: What is the name of your JSON Dto?
